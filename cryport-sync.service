[Unit]
Description = cryport file sync service
After = network.target

[Service]
PIDFile = /run/cryportsyncservice/cryportsyncservice.pid
User = gitlab-runner
Group = gitlab-runnner
WorkingDirectory = /opt/cryportsyncservice
ExecStartPre = /bin/mkdir /run/cryportsyncservice
ExecStartPre = /bin/chown -R gitlab-runner:gitlab-runner /run/cryportsyncservice
ExecStart = /bin/bash /opt/cryportsyncservice/cryport-rsync.sh
ExecReload = /bin/kill -s HUP $MAINPID
ExecStop = /bin/kill -s TERM $MAINPID
ExecStopPost = /bin/rm -rf /run/cryportsyncservice
PrivateTmp = true

[Install]
WantedBy = multi-user.target